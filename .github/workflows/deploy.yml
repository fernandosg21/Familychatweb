name: Deploy to Vercel

on:
  push:
    branches:
      - claude/family-messaging-app-ri1a7q

jobs:
  trigger-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Call Vercel Deploy Hook
        run: |
          curl -fsS -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"
